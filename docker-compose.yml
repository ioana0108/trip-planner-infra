version: '3.8'

services:
  auth-service:
    build:
      context: ../trip-planner-auth-service/TripPlanner.AuthService
    ports:
      - "8081:8080"

  trip-service:
    build:
      context: ../trip-planner-trip-service/TripPlanner.TripService
    ports:
      - "8082:8080"